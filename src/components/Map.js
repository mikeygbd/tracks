import React from 'react';
import {
  Text,
  StyleSheet,
} from 'react-native';

const Map = ({}) => (
  <>
    <Text>I'm Map</Text>
  </>
);

export default Map;

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
